FUNZIONALITà DA IMPLEMENTARE:

- analisi complessità


----------------------
TESTING
1) controllare a tutti gli element che prendono position in ingresso il metodo validate ------OK
2) try catch sul metodo validate e decommentare il test di inserimento in posizione non valida ----- OK
3) cancella i metodi che fanno solo super  ----- OK
4) verifica se l a str stampa -------- OK
5) after e before non devono restituire lo stesso elemento  --------OK
6) aggiungi main ---- OK
7) aggiungere il path alle variabili d ambiente del interprete in modo da importare semplicemente i file -------- OK
8) aggiungere al test una divisione in paragrafi per orientarsi meglio osservango l' output del test ----OK
9) Controllare la insert di scoreBoard, validate! -----OK
